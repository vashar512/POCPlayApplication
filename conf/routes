# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.HomePageController.renderPage
GET     /hello/:age                 controllers.RequestParamExamplePageController.renderPage(age: Int)
GET     /assets/*file               controllers.Assets.at(path="/public", file)